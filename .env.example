# OpenClaw installation root (auto-detected from $HOME)
# OPENCLAW_ROOT=~/.openclaw

# OpenClaw workspace root
# OPENCLAW_WORKSPACE=~/.openclaw/workspace

# Frontend API URL - 前端 API 基础地址
# 默认连接本地后端 http://localhost:3001
# 如果后端 API 不可用，前端会自动降级使用 mock 数据
NEXT_PUBLIC_API_URL=http://localhost:3001
